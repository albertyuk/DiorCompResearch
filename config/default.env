# Maison Monitor — team defaults (committed on the repo owner's instruction).
# A local .env in the repo root overrides these.
#
# The Anthropic key is NOT committed: GitHub push protection blocks sk-ant-…
# patterns. start.command asks for it once on first run and saves it to .env.
# (Repo owner: to commit it anyway, disable push protection in the repo's
# Settings → Code security, then fill it in here.)
TIKHUB_API_KEY=g4gFAsEtzn2MhVSxRaGq5w/nMDmCSqVO3z6GtHzeU/y/il3hd0qPdRukMw==
# ANTHROPIC_API_KEY=
